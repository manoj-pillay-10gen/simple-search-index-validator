{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "description": "The keyword tokenizer tokenizes the entire input as a single token.",
  "properties": {
    "type": {
      "type": "string",
      "enum": ["keyword"],
      "markdownDescription": "Human-readable label that identifies this tokenizer type. Value must be `keyword`."
    }
  },
  "required": ["type"]
}
