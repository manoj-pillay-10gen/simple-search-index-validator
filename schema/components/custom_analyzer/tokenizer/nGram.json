{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "components/custom_analyzer/tokenizer/nGram.json",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "const": "nGram",
      "description": "Human-readable label that identifies this tokenizer type."
    },
    "minGram": {
      "type": "integer",
      "description": "Number of characters to include in the shortest token created.",
      "minimum": 0
    },
    "maxGram": {
      "type": "integer",
      "description": "Number of characters to include in the longest token created.",
      "minimum": 0
    }
  },
  "required": ["type", "maxGram", "minGram"],
  "additionalProperties": false
}
