{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "fullIndex.json",
  "type": "object",
  "allOf": [
    {
      "$ref": "components/baseIndex.json"
    }
  ],
  "description": "Define your Atlas Search Index",
  "properties": {
    "mappings": true,
    "synonyms": true,
    "analyzer": true,
    "searchAnalyzer": true,
    "analyzers": true,
    "storedSource": true,
    "name": {
      "$ref": "components/common.json#/definitions/non-empty-string",
      "markdownDescription": "**Specify a name for the index**. \n\n In each [namespace](https://www.mongodb.com/docs/manual/reference/limits/#faq-dev-namespace), names of all indexes in the namespace must be unique. If omitted, defaults to `default`.",
      "description": "Specify a name for the index.",
      "defaultSnippets": [
        {
          "label": "index name",
          "body": "default"
        }
      ],
      "type": "string"
    },
    "database": {
      "$ref": "components/common.json#/definitions/non-empty-string",
      "description": "Human readable label that identifies the database to contain the index."
    },
    "collectionName": {
      "$ref": "components/common.json#/definitions/non-empty-string",
      "description": "Human readable label that identifies the collection being indexed."
    }
  },
  "required": ["mappings", "collectionName", "database", "name"],
  "additionalProperties": false
}